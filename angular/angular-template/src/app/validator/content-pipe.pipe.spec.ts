import { ContentPipePipe } from './content-pipe.pipe';

describe('ContentPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ContentPipePipe();
    expect(pipe).toBeTruthy();
  });
});
